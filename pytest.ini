[pytest]
testpaths = src/tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning

markers =
    integration: tests that require network/database
    unit: pure logic tests, no external dependencies